- <details> <summary>mkdir command (a bit complex)</summary>
    
```bash
mkdir -p /var/log/apache2
```
* **mkdir** = make a directory
* **-p** = create parent directories as needed, and don’t throw an error if it already exists
</details>
